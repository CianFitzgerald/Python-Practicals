3(a)By including this line you are essentially calling the function again. Therefore the user will be prompted to enter two number due to the original printmax() line, then by including the print(printmax()) line the user will be prompted to enter two numbers again and the function and the largest number will be returned.3(b)There is no output as the function is not being called with any arguments.3(c)including the line print(printmax) results in the following output: <function printmax at 0x119169dc0> This is the default representation of a function object. It provides you with a name and a unique id which is the memory address of the function.